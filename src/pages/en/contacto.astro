---
import ContactPage from '../../components/ContactPage.astro';

/** Prerender: peranto.app in production is GitHub Pages (static only). */
export const prerender = true;
---

<ContactPage lang="en" />
