---
import ContactPage from '../../components/ContactPage.astro';

/** Prerender: peranto.app en producción es GitHub Pages (solo estático). */
export const prerender = true;
---

<ContactPage lang="es" />
